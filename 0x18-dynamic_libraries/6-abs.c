#include "holberton.h"

/**
 *_abs - Absolute
 *@c: is entry point
 *Return: 0
 **/
int _abs(int c)
{
	if (c < 0)
	{
		c = c * (-1);
		return (c);
	}
	else
	{
		if (c == 0)
		{
			return (c);
		}
		else
		{
			return (c);
		}
	}
}
